#ifndef LED_CFG_H
#define LED_CFG_H


typedef enum{
    GND,
    LED_WARNING,
    LED_DOOROPEN,
    LED_ERROR,
    // LED num => always have the number of element in enum
    GND_PIN,
    LED_LEN
}LED_names_t;







#endif
